{
   "version": 2,
   "builds": [
      {
         "src": "*.js",
         "use": "@vercel/node"
      }
   ],
   "routes": [
      { "src": "/uploads/(.*)", "dest": "/uploads/" },
      {
         "src": "/(.*)",
         "dest": "/"
      }
   ]
}
